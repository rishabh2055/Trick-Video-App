import { ShowHideSidemenuDirective } from './show-hide-sidemenu.directive';

describe('ShowHideSidemenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideSidemenuDirective();
    expect(directive).toBeTruthy();
  });
});
